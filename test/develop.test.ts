import { DataX } from '../src';
import { expect, it, tests } from './helper';

tests('DataX', () => {
  it('DataX.compile(code)', () => {
    const result = DataX.compile(`
      @name 1
    `);
  });
});
